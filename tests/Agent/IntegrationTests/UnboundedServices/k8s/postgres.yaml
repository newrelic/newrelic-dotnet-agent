# Standard License Header
# Kubernetes Deployment and Service for Postgres
apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: unbounded-services
spec:
  replicas: 1
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
    spec:
      containers:
      - name: postgres
        image: dotnetunboundedservices.azurecr.io/postgres:latest
        ports:
        - containerPort: 5432
        envFrom:
        - secretRef:
            name: unboundedservices-secrets
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: unbounded-services
  annotations:
    service.beta.kubernetes.io/azure-load-balancer-resource-group: integration-test-services
    service.beta.kubernetes.io/azure-pip-name: dotnet-unbounded-services-k8s
spec:
  type: LoadBalancer
  loadBalancerIP: 4.246.122.173
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: postgres
