name: Performance Tests

on:
  workflow_dispatch:

jobs:
  performance-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Performance tests not yet implemented."
